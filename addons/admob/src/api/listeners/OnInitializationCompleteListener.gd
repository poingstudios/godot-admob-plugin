class_name OnInitializationCompleteListener

var on_initialization_complete : Callable

func _init(on_initialization_complete := Callable()):
	self.on_initialization_complete = on_initialization_complete
