# DebugGeography

定义用于测试同意流程的模拟调试地理位置。

## 值

=== "GDScript"
    ```gdscript
    # Access via DebugGeography.Values
    enum Values {
        DISABLED = 0,
        EEA = 1,
        NOT_EEA = 2
    }
    ```

=== "C#"
    ```csharp
    public enum DebugGeography
    {
        Disabled = 0,
        Eea = 1,
        NotEea = 2
    }
    ```
