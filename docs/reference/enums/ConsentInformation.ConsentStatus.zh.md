# ConsentInformation.ConsentStatus

指示用户的同意状态。定义在 [`ConsentInformation`](../classes/ConsentInformation.md) 类中。

## 值

=== "GDScript"
    ```gdscript
    # Access via ConsentStatus
    enum ConsentStatus {
        UNKNOWN = 0,
        NOT_REQUIRED = 1,
        REQUIRED = 2,
        OBTAINED = 3
    }
    ```

=== "C#"
    ```csharp
    public enum ConsentStatus
    {
        Unknown = 0,
        NotRequired = 1,
        Required = 2,
        Obtained = 3
    }
    ```
