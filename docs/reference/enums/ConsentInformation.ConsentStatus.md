# ConsentInformation.ConsentStatus

Indicates the user's consent status. Defined inside the [`ConsentInformation`](../classes/ConsentInformation.md) class.

## Values

=== "GDScript"
    ```gdscript
    # Access via ConsentStatus
    enum ConsentStatus {
        UNKNOWN = 0,
        NOT_REQUIRED = 1,
        REQUIRED = 2,
        OBTAINED = 3
    }
    ```

=== "C#"
    ```csharp
    public enum ConsentStatus
    {
        Unknown = 0,
        NotRequired = 1,
        Required = 2,
        Obtained = 3
    }
    ```
